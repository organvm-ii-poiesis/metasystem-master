# Exact committed-source follow-up protocol

Source6f1270d793217783889b5f22779f34ad93228247; recorded after source integration and before the following exact-commit executions. Historical source323c12f6c17753b0b408a3f0e34265939395d864 and its immutable harnesses/results remain separate.

1. Read all11 changed files at the committed ref; verify Git blob identities and byte equality to the tested local candidate. Compare all75 materialized file blobs against the complete new commit tree. Refuse source mismatch.
2. Execute five fresh processes through the canonical core package test:bench command using pnpm9.15.0. Supply the exact committed source SHA; preserve stdout/stderr and raw JSON. Verify five cohorts,50 measured calls per cohort,10 warmups, same-process equality guards, threshold null, cross-process rounded values. Timings are descriptive in-process compute only.
3. Reuse the historical500-fixture minority sweep, temporal/invalid-config/median probes and17-assertion direct-aggregator protocol, now importing actual TypeScript modules and their installed locked runtime dependencies. New harnesses retain separate names and source-identity guards; original isolated harnesses remain unmodified. Compare policy-sensitive outputs to the preserved baseline. Add explicit assertions that epoch-zero expiry is inactive at the current fixed clock and clears both aggregator stores.
4. Execute the real OSC codec/event-path boundary for zero/single/multiple arguments, replacing only UDPPort.open to avoid live sockets. Preserve original failures and labeled repaired observations; no network or performance-delivery claim.
5. Observe both push and PR GitHub evidence jobs at this exact source SHA, including runner assignment, steps and artifacts. No assigned runner means infrastructure_execution_blocked regardless of local outcomes.

Earlier candidate gate passes may be associated with this identical source manifest, but are not newly retagged executions. New outputs have the exact source SHA. The local Node24.19/TypeScript6.0.3 environment differs from workflow Node20.
